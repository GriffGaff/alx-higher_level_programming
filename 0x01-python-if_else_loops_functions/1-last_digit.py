#!/usr/bin/python3
import random
number = random.randint(-10000, 10000)
print(f"Last digit of {number:d} is {number:d[-1]}")
        if {number:d[-1]} > 5:
            print(f"and is greater than 5"}
        elif{number:d[-1]} < 6 and {number:d[-1]} is not == 0:
            print(f"and is less than 6 and not 0")
        else{number:d[-1]} == 0:
            print(f"and is 0")
